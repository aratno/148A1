class Player:
    def __init__(self, name, number):
        self.name = name
        self.number = number
    
    def strategy(game_state):
        
        move = f(game_state)
        #move is SOME function of the given game state
        
        return move